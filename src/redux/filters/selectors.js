export const selectLocation = state => state.filters.draft.location;

export const selectForm = state => state.filters.draft.form;

export const selectEngine = state => state.filters.draft.engine;

export const selectTransmission = state => state.filters.draft.transmission;
